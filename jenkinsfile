pipeline {
    agent any
    
    stages {
        
        stage('Build') {
            steps {
                // Build your project (replace with your actual build command)
                sh 'docker build -t 22424025 .'
            }
        }
        
    }
}
